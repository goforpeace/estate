rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Helper function to check if a user is signed in.
    function isSignedIn() {
      return request.auth != null;
    }

    // Helper function to check if the signed-in user is an admin.
    // The admin's email is hardcoded for this example.
    function isAdmin() {
      return isSignedIn() && request.auth.email == 'admin@estateflow.com';
    }

    // Helper function to check if the user belongs to the tenant.
    // It checks if the user's profile document has a matching tenantId.
    function isTenantMember(tenantId) {
      return isSignedIn() && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.tenantId == tenantId;
    }
    
    // Admins can manage all tenant documents and all user documents.
    match /tenants/{tenantId} {
        allow read, write: if isAdmin();
    }
    
    match /users/{userId} {
       allow read, create, update: if isAdmin();
       // Allow tenant members to read their own profile
       allow read: if request.auth.uid == userId;
    }

    // Tenant members can read and write all documents within their own tenant's data.
    match /tenants/{tenantId}/{document=**} {
      allow read, write: if isTenantMember(tenantId);
    }
  }
}
